لجعل مشروعك على **GitHub** يبدو احترافياً، يجب أن يكون ملف الـ `README.md` مختصراً، منظماً، ويركز على القيمة التقنية (Technical Value). إليك النسخة المختصرة والاحترافية التي ستجذب المطورين والمشرفين:

---

# 🛡️ Secure Auth System (PHP & PDO)

نظام متكامل لإدارة المستخدمين مبني بلغة **PHP** مع التركيز على معايير أمن الويب المتقدمة. المشروع مصمم ليكون نموذجاً تعليمياً وعملياً في تأمين تطبيقات الويب ضد الهجمات الشائعة.

## ✨ المميزات التقنية (Core Features)

* **Database layer:** تعامل كامل عبر **PDO** لضمان استقرار واتصال آمن.
* **Password Security:** تشفير كلمات المرور باستخدام خوارزمية **Bcrypt** (المعيار العالمي).
* **Cyber Defense:** حماية مدمجة ضد هجمات **SQL Injection** و **CSRF**.
* **Brute-Force Protection:** نظام تحديد محاولات الدخول الفاشلة (Rate Limiting).
* **Native UI:** واجهة مستخدم عربية/إنجليزية متجاوبة ومصممة بـ **CSS مخصص** بالكامل (بدون مكتبات خارجية).

## 📂 هيكل المشروع (Project Structure)

```text
├── 📂 database/    # سكربت SQL لإنشاء الجداول
├── 📂 includes/    # الإعدادات (Config) والدوال الأمنية (Functions)
├── 📂 css/ & 📂 js/ # ملفات التصميم والتحقق من المدخلات
├── 📜 index.php     # بوابة تسجيل الدخول (Login)
├── 📜 register.php  # صفحة إنشاء الحساب (Register)
└── 📜 home.php      # لوحة التحكم المحمية

```

## 🚀 التنصيب السريع (Quick Start)

إذا كنت تستخدم نظام **Kali Linux** أو أي خادم Apache:

1. **نسخ المشروع:**
```bash
git clone https://github.com/username/project-name.git
sudo cp -r project-name /var/www/html/

```


2. **إعداد قاعدة البيانات:**
* أنشئ قاعدة بيانات باسم `web_security_db`.
* استورد ملف `database/database.sql`.


3. **ضبط الإعدادات:**
* قم بتعديل بيانات الاتصال في ملف `includes/config.php`.



## 🔒 الممارسات الأمنية المطبقة (Security Overview)

### 1. الوقاية من SQL Injection

يتم استخدام الاستعلامات المُجهزة (Prepared Statements) بشكل حصري:

```php
$stmt = $pdo->prepare("SELECT * FROM users WHERE username = ?");
$stmt->execute([$username]);

```

### 2. تأمين كلمات المرور

لا يتم تخزين كلمة المرور كنص أبداً؛ نستخدم وظائف التجزئة (Hashing):

```php
$hash = password_hash($password, PASSWORD_DEFAULT); // التخزين
password_verify($password, $user['password_hash']); // التحقق

```

### 3. حماية CSRF

توليد رمز فريد (Token) لكل جلسة عمل لمنع تزوير الطلبات عبر المواقع.

## 👥 بيانات التجربة (Test Credentials)

* **Username:** `admin`
* **Password:** `Admin123!`

---

### 👨‍💻 المساهمة والتطوير

المشروع مفتوح المصدر للمساهمة. يمكنك عمل **Fork** وإضافة ميزات مثل (نظام الصلاحيات، استعادة كلمة المرور، أو 2FA).

**هل تود أن أقوم بتجهيز ملف `config.php` بشكل نهائي ليناسب هذا الهيكل الجديد؟**
